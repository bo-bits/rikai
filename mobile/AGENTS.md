# Expo HAS CHANGED

This project is pinned to **Expo SDK 55** (kept Expo Go-compatible — no custom
native modules). Read the exact versioned docs at
https://docs.expo.dev/versions/v55.0.0/ before writing any code.
